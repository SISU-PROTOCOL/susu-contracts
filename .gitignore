# Build artifacts
target/
**/*.rs.bk

# Soroban SDK test snapshots — regenerated by `cargo test` and mostly noise in
# review. Assertions live in the test source, not in these files.
**/test_snapshots/

# Soroban / Stellar CLI local state
.soroban/
stellar/
*.wasm

# Environment — never commit real values
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
